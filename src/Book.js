function Book(title, author, page, isDoneReading, coverColor) {
    this.title = title;
    this.author = author;
    this.page = page;
    this.isDoneReading = isDoneReading;
    this.coverColor = coverColor;
  }

export default Book;